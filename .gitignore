# User data — never commit
data/
.env
HH_Auto
HH_Auto/

# Internal dev journal (not for public repo)
DEVLOG.md

# Historical archive — contains personal data from dev iterations
_docs/

# Runtime artifacts
debug_screenshots/
logs/
__pycache__/
*.pyc
*.pyo
.DS_Store

# Test sandbox (mimics user data dir for subagent / local testing)
sandbox/

# Python environment
venv/
.venv/
*.egg-info/

# Phase 2 design artifacts — not ready for public repo yet
design/

# User dropbox — personal insights/files passed to Claude, never committed
user artifacts/

# Leftovers from legacy paths (belt + suspenders)
hh_cookies.json
llm_cache.json
applied_log.json
pending_actions.json
.claude/
!data/.gitkeep
